namespace FrameForge.App;

/// <summary>
/// Placeholder assembly produced on non-Windows CI hosts.
/// The real WPF application is compiled on Windows.
/// </summary>
public static class NonWindowsStub
{
    public static string Message =>
        "FrameForge.App requires Windows to build the WPF UI. Core libraries and tests are fully available cross-platform.";
}
